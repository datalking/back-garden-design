const unit = 100;

const durations = {
  'transition-duration-faster': `50ms`,
  'transition-duration-fast': `${unit}ms`,
  'transition-duration-base': `${unit * 2}ms`,
  'transition-duration-slow': `${unit * 3}ms`,
  'transition-duration-slower': `${unit * 5}ms`,
};

export { durations };
