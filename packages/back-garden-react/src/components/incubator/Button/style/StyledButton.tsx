// import styled from 'styled-components';

// interface StyledButtonProps {
//   disable?: boolean;
// }

// export const StyledButton = styled.button<StyledButtonProps>`
//   border-radius: 3px;
//   padding: 0.25em 1em;
//   margin: 0 1em;
//   background: transparent;
//   color: ${({ disabled, theme }) => (disabled ? theme.colors.disabledColor : theme.colors.primaryColor)};
//   border: 2px solid ${({ disabled, theme }) => (disabled ? theme.colors.disabledColor : theme.colors.primaryColor)};
// `;
